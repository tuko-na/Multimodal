# 最速解答システム — 要件定義書

> **プロジェクト名**: Multimodal 最速解答システム
> **作成日**: 2026-03-10
> **目的**: 画面上に表示された問題を最速で読み取り、AI推論で解答を返すシステム

---

## 1. システム概要

画面上の特定領域をキャプチャし、マルチモーダルAIに送信して解答を得る、
エッジコンピューティング型の超低レイテンシ解答パイプライン。

### 設計思想

- **「解答の最速化」が唯一の目標**
- ボトルネックを階層ごとに特定・排除するインフラ最適化アプローチ
- Pythonは**指揮官（グルー言語）**として使用し、重い処理はネイティブライブラリ/GPUに委譲

### レイテンシ目標（SLA）

| 処理区間 | 目標値 | 備考 |
|---|---|---|
| **内部処理**（キャプチャ〜前処理完了） | **16ms以内** | 60FPS相当 |
| **E2E（クラウドAPI / Gemini Flash）** | **1.5〜2秒以内** | ネットワーク往復時間を含む |
| **E2E（ローカル推論 / MLX）** | **300〜500ms以内** | ネットワーク遅延ゼロ |

> [!WARNING]
> クラウドAPI構成で300ms以内は物理法則（ネットワーク往復時間）的に不可能。環境ごとに現実的な目標を設定すること。

---

## 2. アーキテクチャ — 処理パイプライン

```
画面キャプチャ → 固定座標で切り抜き → リサイズ/軽量化 → AI推論 → オーバーレイ表示
```

### 2.1 処理フェーズ

| フェーズ | 説明 |
|---|---|
| **キャリブレーション** | テスト開始前に問題領域の座標をマウスで範囲指定し、固定値として保存 |
| **実行ループ** | 固定座標での切り抜き → 前処理 → API送信/ローカル推論 を非同期で繰り返す |

> [!IMPORTANT]
> 毎フレーム動的に領域検知するのは**禁止**。事前キャリブレーションによる固定値処理に落とし込むこと。

---

## 3. 最適化の6階層

### 3.1 クラスタレベル — 分散処理

- **構成**: 不採用（スタンドアローン一択）
- **理由**: ノード間通信の遅延がボトルネックになるため、1台で完結するエッジコンピューティング構成とする

### 3.2 コンピュータレベル — ハードウェア活用

- **構成**: Apple Silicon（M2/M3）のユニファイドメモリを極限活用
- **技術**: CPU→GPU間の**ゼロコピー転送**（ポインタ渡し）
- **GPU活用**: Metal Performance Shaders (MPS) を明示的に指定

### 3.3 Python処理系・コンパイラレベル

- **構成**: 標準CPython + Numba/Cython の部分適用
- **方針**: AI系ライブラリとの互換性を維持しつつ、重い自作関数だけを `@jit` でネイティブコンパイル
- PyPy は AI ライブラリとの相性問題のため不採用

### 3.4 ライブラリレベル — 最速モジュール選定

| 機能 | 採用技術 | 理由 |
|---|---|---|
| 画面取得 | `ScreenCaptureKit`（`pyobjc` 経由） | macOSネイティブAPI。`mss` より高速 |
| 画像処理 | `OpenCV (cv2)` + `NumPy` | C/C++実装。NumPy配列と直接連携 |
| AI推論（クラウド） | `aiohttp` + `Gemini Flash` | ノンブロッキング通信 + 軽量高速モデル |
| AI推論（ローカル） | `MLX` + 軽量VLM（例: Qwen-VL） | ネットワーク遅延ゼロの究極構成 |
| 出力UI | `PyQt6` または `PySide6` | 背景透過オーバーレイウィンドウ |

### 3.5 コーディングレベル — アルゴリズムとメモリ管理

| ルール | 詳細 |
|---|---|
| **非同期処理** | `asyncio` で API待ち中に次のキャプチャを並行実行 |
| **ゼロコピースライス** | `image_array[y1:y2, x1:x2]` で参照のみ。新規配列生成禁止 |
| **forループ禁止** | ピクセル単位のPython forループは使用しない。NumPyベクトル化に統一 |
| **メモリ事前割り当て** | `append()` による逐次追加を避け、事前にゼロ埋め配列を確保 |
| **GIL対策** | CPUバウンド → `multiprocessing`、I/Oバウンド → `asyncio`/`threading` |

### 3.6 FFI（他言語バインディング）

- **構成**: Rust + PyO3（最終兵器）
- **適用条件**: Pythonでミリ秒単位のオーバーヘッドが削れない処理のみ
- **対象例**: 画面キャプチャ → グレースケール化 → メモリ書き込み の一連のI/O処理

---

## 4. 入力データの最適化ルール

### 4.1 領域の限定

- 画面全体（フルHD/4K）のキャプチャは**禁止**
- キャリブレーションで決定した固定座標のバウンディングボックスのみを切り抜く

### 4.2 データ圧縮と加工

- グレースケール変換（OpenCV）でデータ量を削減
- AIが文字を認識できるギリギリの解像度までリサイズ
- **解像度の決定方法**: 開発段階でサンプル画像を使い、OCR精度が劣化し始める閾値を特定し**固定値としてハードコード**

### 4.3 解像度チューニングの基準

```
例: 横幅800px → OCR 100%正解
    横幅600px → 漢字が潰れ始める
→ 横幅800pxを定数として採用
```

---

## 5. 実行環境

### 5.1 環境選定

| 環境 | 採否 | 理由 |
|---|---|---|
| **ローカル（ベアメタル）** | ✅ 採用 | ハードウェア限界性能を引き出せる |
| コンテナ（Docker） | ❌ 不採用 | macOS上のVM仮想化レイヤーによるI/Oオーバーヘッドが致命的 |
| カスタムOS | ❌ 不採用 | ドライバ開発コストに対するリターンが不釣り合い |

### 5.2 ローカル環境の利点

- OSネイティブAPI（`ScreenCaptureKit`）を直接叩ける
- ユニファイドメモリ上でゼロコピーでデータをGPU/AIモデルに渡せる
- 仮想化の壁がなく、ハードウェアの限界性能をそのまま活用

---

## 6. AI推論レイヤー

### 6.1 クラウドAPI構成

- **モデル**: Gemini Flash（レスポンス速度特化の軽量モデル）
- **通信**: `aiohttp` によるノンブロッキング非同期リクエスト
- **禁止**: `requests` ライブラリの同期通信

### 6.2 ローカル推論構成（究極の最速）

- **フレームワーク**: MLX（Apple Silicon最適化）
- **モデル**: 軽量VLM（Qwen-VL 等）
- **利点**: ネットワーク遅延が完全にゼロ

---

## 7. 出力・表示レイヤー（UIとアンチチート）

### 7.1 オーバーレイUI

- **採用技術**: `PyQt6` または `PySide6`
- **表示方式**: 背景透過の最前面オーバーレイウィンドウ
- **描画方式**: APIレスポンスの完了を待たず、**非同期ストリーミング描画**でトークン単位で逐次表示し、体感待ち時間をゼロにする

### 7.2 アンチチート対策

> [!CAUTION]
> RPA（Selenium、PyAutoGUI等）による自動入力・自動クリックは、bot検知リスクが**極大**のため**絶対に使用禁止**。入力は必ず人間の手で行うこと。

- 解答はオーバーレイで**表示のみ**。操作への介入は一切行わない
- ウィンドウは常時最前面に配置し、問題画面を遮らないよう半透明で小型に設計

---

## 8. プロンプト設計と精度担保

### 8.1 システムプロンプト要件

- **結論ファースト**: 最初に解答を出力し、補足は後に回す
- **認識のズレを瞬時に確認可能**なフォーマットを強制

### 8.2 出力フォーマット（必須）

```
【認識した条件】: （問題の核となる数値や条件を3単語以内で）
【解答】: （最終的な答えのみ）
```

### 8.3 ハルシネーション対策

- 「認識した条件」フィールドにより、AIが問題を読み違えた場合に**人間が即座に検知**可能
- ストリーミング表示と相性が良く、「認識した条件」が先に表示されるため、解答が出る前に誤認識を確認できる

> [!IMPORTANT]
> 「認識した条件」が問題文と一致しない場合は、その解答を破棄して再送信する判断を人間が行うこと。

## 9. キャリブレーション仕様

### 9.1 座標決定

- テスト開始直前、または1問目の画面表示時に実行
- macOSのスクリーンショット機能のようなマウスドラッグで領域指定
- 座標値（`x`, `y`, `width`, `height`）をJSON等で永続化

### 9.2 ダウンスケール倍率

- 開発段階で複数サンプルを使い検証
- OCR精度が維持できる最小解像度を特定
- 固定値（定数）としてプログラムに組み込む

---

## 10. 技術スタック（最終構成）

```
┌─────────────────────────────────────────────┐
│              Python (asyncio 駆動)            │
├─────────────────────────────────────────────┤
│ キャプチャ層  │ ScreenCaptureKit (pyobjc)     │
├──────────────┼──────────────────────────────┤
│ データ加工層  │ NumPy + OpenCV                │
│              │ 固定座標ゼロコピースライス       │
├──────────────┼──────────────────────────────┤
│ AI推論層     │ aiohttp → Gemini Flash        │
│              │ または MLX → ローカルVLM        │
├──────────────┼──────────────────────────────┤
│ 出力UI層     │ PyQt6/PySide6 透過オーバーレイ  │
│              │ ストリーミング逐次描画           │
├──────────────┼──────────────────────────────┤
│ 高速化層     │ Numba (@jit) / Rust (PyO3)    │
│ (必要時)     │                               │
└─────────────────────────────────────────────┘
```

---

## 11. 開発ロードマップ

1. **ベンチマーク**: `mss`/`Pillow` で画面一部キャプチャの FPS 限界測定
2. **キャリブレーションモジュール**: マウスで領域指定 → 座標を固定値保存
3. **前処理パイプライン**: 固定座標切り抜き → グレースケール → リサイズ
4. **AI推論接続**: Gemini Flash への非同期送信 + 解答取得
5. **オーバーレイUI**: PyQt6 による透過ウィンドウ + ストリーミング描画
6. **プロンプト最適化**: 出力フォーマット検証 + ハルシネーション率計測
7. **最適化**: ScreenCaptureKit 移行、Numba/Rust による高速化
8. **ローカル推論**: MLX + 軽量VLM によるオフライン構成

---

## 12. 禁止事項・アンチパターン

| 禁止事項 | 理由 |
|---|---|
| 画面全体のキャプチャ | ペイロード過大、通信遅延 |
| 毎フレームの動的領域検知 | 検知処理のオーバーヘッドが致命的 |
| Python for ループでのピクセル操作 | 致命的に遅い |
| `requests` の同期通信 | API待ちでプログラムが停止 |
| Docker/VM上での実行 | 仮想化レイヤーの遅延が致命的 |
| `append()` による逐次メモリ確保 | 頻繁な再アロケーションで遅延 |
| 最重量モデル（Gemini Pro等）の使用 | レスポンス速度が遅い |
| **RPA（Selenium/PyAutoGUI等）** | **bot検知リスクが極大。絶対禁止** |
| 解答の自動入力・自動クリック | 監視システムに検知される。入力は必ず人間が行う |
| クラウドAPIで300ms以内のSLA設定 | 物理法則的に不可能。非現実的な目標は設定しない |
